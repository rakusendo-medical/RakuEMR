# AGENTS.md

精神科病棟を含む入院機能を中心とした電子カルテの**ワイヤーフレーム（モック）**です。
React + TypeScript + MUI + Vite で作っています。

- 実 API・データベースはありません。画面の状態は zustand のストアに持ち、多くはセッション限定（再読み込みで初期データに戻る）です。
- 「保存されない」「API が無い」こと自体は指摘不要です。仕様（SPEC）でモックの制約として明記しています。
- 参照元のシステムは、ドキュメント・コード・コミットのいずれでも「参考システム」と表記します。

## レビューで扱わないこと

次は CI と別手段で確認済みのため、レビューでは指摘しないでください。

- 書式・インデント・命名の好み、Lint
- 型エラー（`node node_modules/typescript/bin/tsc --noEmit` で確認）
- E2E テスト（`e2e/*.spec.ts`。Playwright で確認）
- 既存の未実装ボタン・プレースホルダ（モックの範囲として意図的に残しています）

## レビューの出し方

- 重要なものから最大 5 件程度にしてください。
- 1 件につき 1〜2 文で、該当ファイルと行を添えてください。
- 同じ指摘の繰り返しは 1 件にまとめてください。

## Code Review Rules

### 仕様と実装の一致

画面の振る舞いを変える変更には、対応する仕様の記述が必要です。

- `docs/specs/` の該当ユーザーストーリー（us-XX）に、変更後の振る舞いが書かれていること。書かれていなければ指摘する。
- 仕様を変えた場合、改定履歴（`src/data/revisions.ts`）に当該バージョンの項目があり、その中に「SPEC記載場所」の項目があること。無ければ指摘する。
- 廃止した仕様は削除ではなく取り消し線で残し、経緯（いつ・誰の判断で・なぜ）を添えること。

### 固有名詞

- 外部の電子カルテ製品名・ベンダー名・サービス名を書かない。参照元を指す必要があるときは「参考システム」と表記する。
- 対象は `docs/` 配下、`src/` のコメントと UI 文言、コミットメッセージ。

### UI の決まり

- 確認は MUI のダイアログで行う。`window.confirm` / `window.alert` は使わない。
- 状態を色だけで区別しない。文字か記号を併記する（色覚への配慮）。
- 破棄・削除など戻せない操作は、確認を挟み、ボタンの色で危険度が分かるようにする。

### 表示データの持ち方

- 画面に出す状態は、起点となるデータから導出する。同じ意味の値を複数の場所に持たせない。
  - 例: 病棟マップのバッジは、隔離拘束指示・外出外泊の記録から導出する（ベッドに直接持たせない）。
  - 例: 患者のステータスは、ステータスを付けた最新の診療録から決める。
- 初期データ（`src/data/mockData.ts`）を変えるときは、その値を参照している画面すべてで表示が壊れないこと。

### 入力フォーム

- 保存する範囲と、画面に見えている範囲を一致させる。表示していない項目を一緒に保存しない。
- 変更が無いときは保存操作をさせない（ボタンを無効にする）。空の保存で記録が増えないようにする。
