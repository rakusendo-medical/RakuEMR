# CLAUDE.md

本リポジトリ（RakuEMR）で作業する際の Claude 向けガイドラインです。

## プロジェクト概要

精神科病棟も含む入院機能を中心とした電子カルテのワイヤーフレーム実装。
React + TypeScript + MUI + Vite で構築されています。

## 開発体制（AI コンダクター運用）

本プロジェクトは PM（ユーザー）+ AI 4 セッションの計 5 名体制で進行する。

| ロール | 役割 |
| --- | --- |
| MASTER（別名: S1） | AI リーダー。進捗管理／タスク管理／HANDOVER 漏れ防止／ep・us 整備 |
| S2〜S4 | AI ワーカー。割り当てられた epic／us の実装 |
| PM | ユーザー本人 |

### セッション起動時の役割宣言フロー

セッション開始時、PM から自分のロール（`MASTER` / `S1` / `S2` / `S3` / `S4`）を告げられたら、以下を実施すること:

1. 対応するロール定義ファイル `.claude/roles/<ロール>.md` を読む
   - MASTER（= S1）の場合: `.claude/roles/MASTER.md`（または `S1.md` 経由）
   - ワーカーの場合: `.claude/roles/S<N>.md`（S2〜S4。共通責務は同ディレクトリの `WORKER.md` を参照）
2. `docs/HANDOVER.md` を読み、現状把握
3. `docs/HANDOVER.md`「アクティブセッション」表に自身を登録（既存行があればステータス・最終更新日を更新）
4. 自ロールの責務範囲を超える作業要請を受けた場合は、`docs/HANDOVER.md`「MASTER 待ち事項」へ起票するか、PM へ確認する

ロール宣言が無いまま実装着手しないこと（誤って共有ファイルを単独編集すると衝突の元になる）。

## 固有名詞ポリシー

### 原則

ドキュメント・コード・コミットメッセージ・Issue／PR のいずれにおいても、外部の電子カルテ製品名・ベンダー名・サービス名などの固有名詞は記載しない。
参照元を指す必要がある場合は、一律で **「参考システム」** と表現する。

- ❌ NG 例: 製品名・ベンダー名・サービス名など参照元を特定できる固有名詞
- ✅ OK 例: 「参考システム」「参考システムの〇〇画面」「参考システムマニュアル」

### 適用範囲

- `docs/` 配下のすべてのマークダウン・コメント
- `docs/issues/` のエピック・ユーザーストーリー
- `src/` 配下のコメント・UI 文言・ドキュメント文字列
- コミットメッセージ・PR タイトル／本文・Issue タイトル／本文

### 既存記述に固有名詞を見つけた場合

新規作成時は最初から「参考システム」表記で書く。
既存ドキュメントに固有名詞が残っているのを発見した場合は、勝手に大規模置換せず、ユーザーに対象範囲を確認した上で対応する。

## 参考システムマニュアルの取り扱い

`docs/manuals/` 配下のファイルは **参考システム** に関するもので、`docs/入院機能一覧.xlsx`（および `docs/issues/` のエピック・ストーリー）の参考用マニュアルとして利用する。

### ファイル構成

| ファイル | 用途 |
| --- | --- |
| `docs/manuals/01 基本システム.pdf` | 参考システム本体機能のマニュアル |
| `docs/manuals/02 看護支援オプション.pdf` | 参考システムの看護支援オプションのマニュアル |
| `docs/manuals/参考システムマニュアル対応表.xlsx` | 入院機能一覧の各操作と上記マニュアルのページ範囲を紐付けた対応表 |

### 使い方

- 入院機能の仕様・画面要素・ダイアログ構成を確認したいときは、まず `参考システムマニュアル対応表.xlsx` で対象操作のページ範囲を特定し、該当 PDF の該当ページのみを参照する。
- マニュアル本文を引用・要約する際も、固有名詞は除去し「参考システム」表記で扱う。
- 対応表に列挙された画面・ダイアログ名はそのまま機能名として利用してよい（製品名ではないため）。

## ディレクトリ構成（要点）

```
docs/
├── 入院機能一覧.xlsx           # 入院機能の元データ
├── nursing_care_plan_user_stories.md
├── claude_code_wireframe_prompt.md
├── issues/                     # エピック・ユーザーストーリードラフト
│   ├── README.md
│   ├── manifest.tsv
│   ├── epics/
│   └── user-story/
└── manuals/                    # 参考システムのマニュアル（上記参照）

src/
├── components/                 # 画面コンポーネント
├── features/carePlan/          # 看護過程（看護計画）機能
├── routes/                     # ルーティング
├── layouts/                    # 共通レイアウト
└── theme/                      # MUI テーマ
```

## 開発時の注意

- UI 文言は日本語。製品としての固有名詞は出さない。
- 既存実装のリファクタや一括置換などインパクトの大きい作業は、事前にユーザーに範囲を確認する。
