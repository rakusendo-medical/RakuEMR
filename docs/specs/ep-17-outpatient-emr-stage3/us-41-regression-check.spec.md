# us-41 [外来・共通] 段階 3 リグレッション確認 + ep-17 クローズ

## メタ

| 項目 | 内容 |
| --- | --- |
| 対応エピック | [ep-17](./_epic.md) |
| 対応モック画面 | 全画面（ep-01〜10 / ep-15 / ep-16 のリグレッション網羅） |
| 想定ロール | PM（ブラウザ目視） + MASTER（チェックリスト整備 + 結果反映） |
| ステータス | draft（2026-05-11 着手） |

### 参考システムマニュアル

本 us は最終リグレッション確認のため参考システムマニュアル直接対応なし。各 ep の spec を集合的に参照する。

## ユーザーストーリー

- **As a** PM
- **I want** us-39 / us-40 完了後、ep-01〜10 / ep-15 / ep-16 の主要動線がすべて正常に動作することを確認したい
- **So that** 「カルテ画面 1 ファイル / 1 ルート」の最終形を安心してクローズできる

## 振る舞い

us-39（`/karte-alpha` 撤去）と us-40（旧素材撤去）の双方が tsc + build クリーンの状態でマージ済であることを前提に、PM が下記チェックリストをブラウザで目視確認する。

## 受け入れ基準（AC・PM ブラウザ目視チェックリスト）

### ep-01 病棟マップ

- [ ] **AC-1-1**: `/` 表示 → 病棟マップが正常レンダリング
- [ ] **AC-1-2**: 患者ベッドをダブルクリック → `/karte/:patientId`（inpatient mode）へ遷移
- [ ] **AC-1-3**: カルテヘッダーから「一覧に戻る」 → `/`（病棟マップ）に戻る

### ep-02 入院患者一覧

- [ ] **AC-2-1**: `/patients` 表示 → 入院患者一覧が正常レンダリング
- [ ] **AC-2-2**: 患者行クリック → `/karte/:patientId`（inpatient mode）へ遷移
- [ ] **AC-2-3**: 「一覧に戻る」 → `/patients` に戻る

### ep-03 入退院指示

- [ ] **AC-3-1**: 新カルテ画面（inpatient mode）の `KarteActionBar` から「入院指示」ボタン → `AdmissionOrderDialog` 起動
- [ ] **AC-3-2**: 入院確定後、画面遷移 / 状態反映が ep-16 段階 2 と同じく動作
- [ ] **AC-3-3**: 入院中患者カルテで「退院指示」ボタン → `DischargeOrderDialog` 起動 → 退院確定動作
- [ ] **AC-3-4**: `/admission` の予定カレンダー → 確定済予定の患者クリック → `/karte/:patientId`（inpatient mode）へ遷移

### ep-05 隔離拘束指示

- [ ] **AC-5-1**: 新カルテ画面の診療録タブ右側に `RestraintOrderLinks`（隔離拘束指示リンク群）表示
- [ ] **AC-5-2**: 各リンクから `RestraintOrderDialog` 起動 → 開始/解除/継続/変更の操作
- [ ] **AC-5-3**: `/isolation` の隔離拘束一覧 → 患者行クリック → `/karte/:patientId`（inpatient mode）へ遷移

### ep-06 観察記録 / ep-07 観察ダイアログ

- [ ] **AC-6-1**: 新カルテのフローシートタブ → 隔離拘束タブ → 観察記録の入力 / 一覧 / 取消が動作

### ep-09 患者基本情報

- [ ] **AC-9-1**: 新カルテの「患者情報」タブ → 7 サブタブ（基本情報 / 属性 / 保険 / 連絡先 / 病名 / エピソード / メモ）切替が動作
- [ ] **AC-9-2**: 編集 → 保存 / 未保存検知ダイアログが ep-15 段階 1 と同様に動作

### ep-10 看護実施（フローシート）

- [ ] **AC-10-1**: 新カルテのフローシートタブ → バイタル / IN-OUT / 観察 / 処置 / 看護記録 のいずれも正常表示
- [ ] **AC-10-2**: `/nursing/*` の独立ルート（フローシート単独利用）も動作

### ep-15 外来 EMR 段階 1

- [ ] **AC-15-1**: `/outpatient` → 患者クリック → `/karte/:patientId`（outpatient mode）へ遷移
- [ ] **AC-15-2**: 外来 mode 配色が success（緑系）で表示
- [ ] **AC-15-3**: 看護過程タブが disabled + Tooltip 表示

### ep-16 外来 EMR 段階 2

- [ ] **AC-16-1**: 入院 mode で新カルテ表示 → 7 タブ（診療録 / フローシート / 指示簿 / 指示状況 / 看護過程 / 患者情報 / スケジュール）すべて表示
- [ ] **AC-16-2**: 患者ヘッダーに 8 ピクトグラム / 主治医 / 入院日 / 病名 / 隔離拘束バッジが表示
- [ ] **AC-16-3**: 診療情報パネル（7 サブタブ・固定高さ 140px）が表示
- [ ] **AC-16-4**: 生活歴タイムライン（5 行）が表示
- [ ] **AC-16-5**: 診療録タブの集約タイムライン（カテゴリ / タグ / 期間フィルタ）が動作
- [ ] **AC-16-6**: 指示簿 / 指示状況 / スケジュール各タブで P001〜P003 のデータが表示

### 撤去確認（段階 3 固有）

- [ ] **AC-X-1**: ブラウザで `/karte-alpha/P001` を直接入力 → 404（「ページが見つかりません」）
- [ ] **AC-X-2**: ソースツリーに `src/components/karteAlpha/` が存在しない
- [ ] **AC-X-3**: ソースツリーに `src/components/flowsheet/FlowsheetPage.tsx` が存在しない
- [ ] **AC-X-4**: ソースツリーに `src/components/karte/PatientHeader.tsx` 等の旧素材 5 件が存在しない

## 完了処理（MASTER 担当）

- [ ] PM 目視確認結果を `docs/changes/ep-17-outpatient-emr-stage3.md` の「最終リグレッション結果」節に反映
- [ ] 不具合検出時は: 軽微なら即時修正 → 再目視、深刻なら新規 us 起票 → 修正後再目視
- [ ] 全 AC ✅ 後、HANDOVER の ep-17 を ✅ 完了に移動 + 段階 3 クローズ note 記載
- [ ] エピック評価ページからは既に ep-17 を除外済（2026-05-11 commit `49adef2`）のため、ダッシュボード反映作業は不要

## 補足

- 本 us は **実装 0、目視 + ドキュメント整備のみ**
- 自動テストなしの方針通り、PM ブラウザ目視を一次保証手段とする
- リグレッション網羅範囲が広いため、PM の都合に合わせて分割実施可（例: ep-01 → ep-05 → ep-16 → 残）
